# 工作日志 — Nuwa-Skill 改造为 Personality Insight Agent

**日期**: 2026-05-23
**任务**: 基于 Nuwa-Skill 的 Pipeline 架构，构建通用 Python 人物画像分析 Agent
**成果**: `personality_insight_agent/` Python 包

---

## 1. 调研 Nuwa-Skill

从 [github.com/alchaincyf/nuwa-skill](https://github.com/alchaincyf/nuwa-skill) clone 源码，通读核心文件：

| 文件 | 内容 | 收获 |
|------|------|------|
| `SKILL.md` | 女娲本体，Phase 0-5 完整流程定义 | 理解了入口分流、检查点、降级策略、双Agent精炼的设计意图 |
| `references/extraction-framework.md` | 三重验证方法论 + 表达DNA量化 + 矛盾处理原则 + 质量自检清单 | 直接借鉴为 Skill2/Skill4 的核心逻辑 |
| `references/skill-template.md` | 生成的 SKILL.md 模板 | 理解 Nuwa 的输出格式，确定我们分叉为 JSON 画像而非角色扮演 Skill |
| `examples/steve-jobs-perspective/` | 完整调研→提炼→构建的案例 | 验证了 6-Agent 并行采集 + 三重验证的可行性 |

### Nuwa 可复用设计清单

| 复用项 | 适配方式 |
|--------|---------|
| Phase 0 入口分流 | 直接采用，重写为 Python class-based 实现 |
| 6-Agent 并行采集 | Skill1 改编为"知识库查询 + 文本分片"，非网络搜索 |
| 三重验证(跨域/生成/排他) | 嵌入 Skill2 心智模型识别 + Skill4 框架交叉验证 |
| 句式指纹 + 风格标签 | 直接采用为 Skill3 的分析维度 |
| Phase 1.5/2.5 检查点 | Orchestrator 中实现中间结果质量检查 |
| Phase 4 质量验证 | 改编为 3 项测试（已知/边缘/完整性） |
| Phase 5 双Agent精炼 | 保留为 Skill5 后置工序 |
| 诚实边界 | Skill5 核心输出字段 |
| 矛盾处理(3类) | Skill5  inconsistency detection |
| 冷门人物降级策略 | Orchestrator 降级策略表 |

### Nuwa 不适合直接复用的部分

| 差异点 | Nuwa | 本项目 |
|--------|------|--------|
| 输入 | 人名 → 全网搜索 | 文本/知识库ID → 本地分析 |
| 输出 | SKILL.md (第一人称角色扮演) | JSON + Markdown (第三人称画像报告) |
| 理论支撑 | 无特定心理学框架 | 大五/MBTI/动机/PDP 四框架 |
| 产物用途 | 让 AI 用某人的方式回答问题 | 生成结构化的心理画像报告 |

---

## 2. 设计文档编写

产物: `personality-insight-agent-design.md`

- 定义 5-Skill Pipeline 架构
- 画出完整数据流图
- 确定各 Skill 的输入输出 JSON Schema
- 编写 2 个完整 few-shot example
- 设计 Orchestrator 角色 Prompt + 检查点 + 降级策略
- 规划项目文件结构

耗时约 1h。

---

## 3. Python 代码实现

### 3.1 数据模型 (`models.py`)

- 全部 Pydantic v2 BaseModel
- 定义 InputType/RiskOrientation/PDPStyle 等 20+ 枚举
- 每个 Skill 的 Input/Output 独立模型
- OrchestratorInput/Output + PipelineStep 状态追踪
- 兼容 Python 3.8 (去除 `from __future__ import annotations`，替换 `dict[str, Any]` → `Dict[str, Any]`)

### 3.2 Skill 1: 素材检索与清洗 (`skills/skill1_material_retrieval.py`)

```
Skill1MaterialRetrieval
├── build_system_prompt() → 清洗规则 + 分片规则 + 质量评分
├── build_user_prompt()   → 格式化输入
├── parse_output()       → 解析 JSON → Skill1Output
├── run(llm_callable)     → 完整调用流程
├── pre_clean()           → 本地预处理（正则去噪）
└── quick_assess()        → 本地快速评估（不计入LLM调用）
```

### 3.3 Skill 2: 认知与思维特征 (`skills/skill2_cognitive_profile.py`)

```
Skill2CognitiveProfile
├── system_prompt: 含完整三重验证方法论文档
├── 分析维度: 信息处理/决策模式/风险态度/创造性/归因/时间取向/心智模型
├── parse_output(): 解析认知画像 + 心智模型列表 + TripleCheck
└── 心智模型 ≤ 5 个（宁缺毋滥）
```

### 3.4 Skill 3: 语言风格与情感 (`skills/skill3_language_emotion.py`)

```
Skill3LanguageEmotion
├── system_prompt: 含 Nuwa 句式指纹 + 风格标签 + VAD情感模型
├── 分析维度: 句式指纹(5项)/风格标签(4项)/情感(4项)/修辞(3项)/标志表达
├── parse_output(): 解析 SentenceFingerprint + StyleTags + Emotion + Rhetoric
└── 标志性表达模式必须是人独有的
```

### 3.5 Skill 4: 性格特质推断 (`skills/skill4_personality_inference.py`)

```
Skill4PersonalityInference
├── 输入: Skill2输出 + Skill3输出
├── 推断框架: 大五(OCEAN) → MBTI → 动机(McClelland) → PDP
├── 一致性检验: 框架内 + 框架间 + 信号覆盖
├── bf_to_mbti_approx(): 大五→MBTI 本地近似映射（备用）
└── 每条结论必须有 Skill2/Skill3 的信号作为依据
```

### 3.6 Skill 5: 画像合成 (`skills/skill5_profile_synthesis.py`)

```
Skill5ProfileSynthesis
├── 输入: Skill1-4 全部输出
├── 核心任务: 置信度/核心画像/矛盾检测/洞察/诚实边界/报告生成
├── compute_confidence(): 本地置信度加权计算
├── 输出: JSON 数据 + Markdown 报告
└── 诚实边界是最重要的 section
```

### 3.7 编排器 (`orchestrator.py`)

```
PersonalityInsightOrchestrator
├── analyze(llm_callable, raw_text, person_id) → OrchestratorOutput
├── Phase 0: _classify_input() + _check_degradation()
├── Phase 1: Skill1 (本地预处理 或 LLM调用)
├── Phase 2-4: Skill2 → Skill3 → Skill4 顺序调用
├── Phase 5: Skill5 聚合 + compute_confidence 应用
├── 检查点: material_quality < 0.4 → 终止
│           skill_quality < 0.5 → 降低 confidence
└── 降级阈值: MIN_WORDS_FULL=5000, MIN_WORDS_PARTIAL=1000
```

### 3.8 工具 (`tools/`)

**text_cleaner.py**:
- `clean_text()` — 基础清理
- `remove_timestamps()` — 去除 4 种时间戳格式
- `remove_system_messages()` — 去除系统消息
- `replace_urls()` — URL → `<URL>`
- `remove_emoji_only_lines()` — 去除纯表情行
- `extract_messages()` — 聊天记录 → 结构化消息列表
- `count_words()` — 中英文混合字数统计
- `full_clean()` — 完整清洗流水线

**quality_checker.py** (借鉴 Nuwa 检查清单):
- `check_mental_models()` — 数量 1-5 检查
- `check_evidence_coverage()` — 递归检查所有 evidence 字段
- `check_honesty_boundary()` — 已知/不确定/未知 三项齐全
- `check_contradictions()` — 至少 1 处矛盾被记录
- `check_confidence_granularity()` — 置信度不超 0.9
- `run_all_checks()` — 一键运行 + 生成 QualityReport

### 3.9 Few-shot 样例

- `few_shot_examples/chat_examples.json` — 2 个完整示例：
  - "技术讨论场景—渐进决策者"（含 Skill2/3/4 预期输出）
  - "公众号文章场景—感性叙事者"（含 Skill2/3/4 预期输出）
- `few_shot_examples/article_examples.json` — 2 个完整示例：
  - "科技博客—分析型思维"
  - "个人反思—内省型"

### 3.10 入口 (`main.py`)

```python
analyze_personality(llm_call, text, person_id) → dict
quick_analyze(llm_call, text) → dict
compare_personalities(llm_call, texts) → list[dict]
```

CLI 接口 + dry_run 模式用于 Pipeline 结构测试。

---

## 4. 踩坑记录

| 问题 | 原因 | 解决 |
|------|------|------|
| Pydantic 报 `type object is not subscriptable` | Python 3.8 + Pydantic 2.13 不支持 PEP 585 语法 (`dict[str, Any]`) | 替换为 `Dict[str, Any]`、`List[...]`、`Optional[...]`，models.py 移除 `from __future__ import annotations` |
| sed 引入 null bytes | `sed -i` 在特定环境下损坏文件 | `tr -d '\0'` 修复所有文件 |
| `from __future__ import annotations` 误插入 docstring | 批量处理脚本逻辑缺陷 | 手动修正各文件 |
| 模块找不到 | 目录名 `personality-insight-agent` 含连字符 | 重命名为 `personality_insight_agent` |

---

## 5. 最终交付物

```
personality_insight_agent/
├── __init__.py                     # 公开 API
├── models.py                       # 20+ 枚举 + 15+ Pydantic 模型
├── orchestrator.py                 # Pipeline 编排器
├── main.py                         # 入口 + CLI
├── skills/
│   ├── __init__.py
│   ├── skill1_material_retrieval.py
│   ├── skill2_cognitive_profile.py
│   ├── skill3_language_emotion.py
│   ├── skill4_personality_inference.py
│   └── skill5_profile_synthesis.py
├── tools/
│   ├── __init__.py
│   ├── text_cleaner.py
│   └── quality_checker.py
└── few_shot_examples/
    ├── chat_examples.json
    └── article_examples.json

共计: 15 个 Python 文件，2 个 JSON 数据文件
代码行数: ~2500 行
```

---

## 6. 待办

- [ ] 真实 LLM API 接入测试（目前仅 dry_run 验证通过）
- [ ] 补充更多 few-shot 样例（冲突场景、多语言、多人对比）
- [ ] MCP Server 包装，使其可作为外部工具挂载
- [ ] 向量知识库集成（Milvus/Chroma），支持增量更新画像
- [ ] 时序对比功能（同一人不同时期的画像变化追踪）

---

## 7. 架构升级 — 三大前沿组件集成 (下午)

基于上午完成的 `personality_insight_agent` v1.0，进行了全面架构升级，融入三个前沿组件。

### 7.1 组件一: Corpus2Skill — 构筑"记忆宫殿"

**核心理念**: 从"检索"到"导航"。为 Agent 提供可交互的层级知识地图。

**实现文件**:
- `corpus2skill/models.py` — KnowledgeTreeNode / CorpusIndex / SkillDirectoryTree / NavigationContext / NavigationPlan
- `corpus2skill/builder.py` — 离线流水线 CorpusTreeBuilder (扫描→分类→chunk→LLM摘要→INDEX层→根节点)
- `corpus2skill/navigator.py` — 在线导航 KnowledgeNavigator (路径规划→导航执行→上下文检索→回溯/上溯)

**技术要点**:
- 根节点 SKILL.md → 中间节点 INDEX.md (按6大ContentCategory组织) → 叶节点具体文档
- 文件自动分类: 文件名模式 + 目录名模式 → writings/conversations/expressions/external/decisions/timeline
- 导航式检索: `plan_route("重大商业决策")` → Step1: decisions分类 → Step2: writings分类 → Step3: conversations分类
- 支持 O(1) 节点查询 + 面包屑路径 + 树结构可视化

### 7.2 组件二: Agentic RAG — 升级"研究者"

**核心理念**: 将被动"收集者"升级为 `自主规划 → 深度检索 → 反思迭代` 的智能体。

**实现文件**:
- `agentic_rag/models.py` — ResearchPlan/Task, RetrievalResult, ReflectionResult, AgentState/Action, ResearchLoopConfig
- `agentic_rag/planner.py` — ResearchPlanner (任务分解 + 检索路径规划 + 动态replan)
- `agentic_rag/retriever.py` — DeepRetriever (多跳检索 + 混合检索 vector/navigate/keyword + 重排序)
- `agentic_rag/reflector.py` — ResearchReflector (信息充分性评估 + 冲突检测 + 缺口分析 + 继续/停止决策)
- `agentic_rag/research_agent.py` — AgenticResearchAgent (完整循环 + Nuwa 6-Agent 并行兼容)

**技术要点**:
- 状态机: IDLE → PLANNING → RETRIEVING ⇄ REFLECTING → SYNTHESIZING → DONE
- 每个 Agent 内部自主循环，可配置 max_rounds / early_stop_quality / timeout
- 兼容 Nuwa 6-Agent 并行: `AgenticResearchAgent.run_parallel_agents()` 接口
- 无向量库时自动降级为 LLM 知识检索
- 冲突类型: temporal(时间性) / spatial(场景性) / essential(本质性)

### 7.3 组件三: "双引擎"验证 — 确保客观性

**核心理念**: 内部三重验证 + 外部事实核查 = 双引擎驱动的客观性闭环。

**实现文件**:
- `verification/models.py` — FactCheckItem, VerificationResult, ConflictResolution, ArbitrationResult
- `verification/fact_checker.py` — FactCheckAgent (声明提取→RAG证据检索→对比评估→verified/contradicted/uncertain)
- `verification/arbitrator.py` — DualEngineArbitrator (5策略仲裁矩阵 + 深度研判 + 置信度调整)

**仲裁策略矩阵** (INTERNAL置信度 × EXTERNAL证据强度):

| 内部\外部 | confirmed | likely | uncertain | contradicted | not_found |
|-----------|-----------|--------|-----------|-------------|-----------|
| **高** (≥0.7) | merge | merge | prefer_internal | flag_as_disputed | prefer_internal |
| **中** (0.4-0.7) | merge | merge | flag_as_disputed | deep_dive | prefer_internal |
| **低** (<0.4) | prefer_external | prefer_external | flag_as_disputed | prefer_external | flag_as_disputed |

### 7.4 Orchestrator V2 整合

**文件**: `orchestrator_v2.py` — PersonalityInsightOrchestratorV2

升级后的完整 Pipeline:
```
Phase 0   → 入口分流 (纯文本/知识库/混合)
Phase 0.5 → Corpus2Skill 知识树构建 (离线流水线)
Phase 0.6 → Agentic RAG 6-Agent 自主研究 (并行)
Phase 1   → Skill1 素材检索与清洗
Phase 2   → Skill2 认知与思维特征
Phase 3   → Skill3 语言风格与情感
Phase 4   → Skill4 性格特质推断
Phase 5   → Skill5 画像合成
Phase 6   → 双引擎验证 (事实核查 + 仲裁)
Phase 7   → 最终报告 (含仲裁建议 + 扩展Markdown)
```

**两种模式**:
1. `analyze()` — 纯文本模式，兼容 V1 API
2. `analyze_with_corpus()` — 含私有知识库的完整模式

### 7.5 测试验证

**文件**: `tests/test_integration_v2.py` — 6 项端到端集成测试，全部通过:

| # | 测试项 | 状态 |
|---|--------|------|
| 1 | Corpus2Skill 离线流水线 (2文档→知识树→导航器→路径规划) | PASS |
| 2 | Agentic RAG 循环 (Planner→Retriever→Reflector→ResearchAgent) | PASS |
| 3 | 双引擎验证 (FactChecker→Arbitrator, 含人工制造冲突) | PASS |
| 4 | V2 Pipeline 纯文本模式 (5-Skill全流程) | PASS |
| 5 | V2 Pipeline 含知识库模式 (Corpus2Skill+Agentic RAG+双引擎) | PASS |
| 6 | 仲裁决策矩阵 (7组 conf×evidence 策略选择) | PASS |

### 7.6 最终交付物更新

```
personality_insight_agent/
├── __init__.py                     # v2.0.0, 暴露全部 API
├── models.py                       # 已有
├── orchestrator.py                 # V1 (保持不变)
├── orchestrator_v2.py              # NEW — V2 编排器
├── main.py                         # UPDATED — +analyze_with_private_corpus()
├── corpus2skill/                   # NEW — 组件一 (3 files)
│   ├── __init__.py / models.py / builder.py / navigator.py
├── agentic_rag/                    # NEW — 组件二 (5 files)
│   ├── __init__.py / models.py / planner.py / retriever.py / reflector.py / research_agent.py
├── verification/                   # NEW — 组件三 (3 files)
│   ├── __init__.py / models.py / fact_checker.py / arbitrator.py
├── skills/                         # 已有 (5 skills)
├── tools/                          # 已有 (text_cleaner, quality_checker)
├── few_shot_examples/              # 已有
└── tests/
    ├── __init__.py
    └── test_integration_v2.py      # NEW — 6项集成测试
```

新增: 13 个 Python 文件, ~2800 行代码
总计: 28 个 Python 文件, ~5300 行代码

---

## 8. 待办 (更新)

- [ ] 真实 LLM API 接入测试（目前仅 dry_run + mock 验证通过）
- [ ] FAISS 向量库实际对接 (已有 D:\api\src\core.py 的 RAG 代码可复用)
- [ ] MCP Server 包装，使其可作为外部工具挂载
- [ ] 补充更多 few-shot 样例（冲突场景、多语言、多人对比）
- [ ] 时序对比功能（同一人不同时期的画像变化追踪）
- [ ] 异步并行: Agentic RAG 6-Agent 真正并行 (目前 Python 层顺序执行)
- [ ] 增量知识树更新 (避免每次全量重建 Corpus2Skill)
- [ ] 向量知识库集成（Milvus/Chroma），支持增量更新画像
